# ============================================================================
# Cronicorn Production Environment Configuration
# ============================================================================
#
# This file contains environment variables for PRODUCTION deployments.
# Copy this to your Dokploy production project's environment variables.
#
# ⚠️  SECURITY: Use strong, unique values for all secrets!
# ⚠️  SECURITY: Never use staging/development credentials in production!
# ============================================================================

# ----------------------------------------------------------------------------
# 🐳 DOCKER IMAGE CONFIGURATION
# ----------------------------------------------------------------------------
IMAGE_REGISTRY=ghcr.io/weskerllc/cronicorn
IMAGE_TAG=v1.6.1  # IMPORTANT: Use specific version tags, not 'latest'

# ----------------------------------------------------------------------------
# 🗄️  DATABASE
# ----------------------------------------------------------------------------
POSTGRES_USER=production_user
POSTGRES_PASSWORD=CHANGE_ME_TO_VERY_SECURE_PASSWORD
POSTGRES_DB=cronicorn

# ----------------------------------------------------------------------------
# 🔐 AUTHENTICATION
# ----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=CHANGE_ME_TO_VERY_SECURE_SECRET_MIN32CHARS

# Update these URLs to match your production domain
BETTER_AUTH_URL=https://api.yourdomain.com
WEB_URL=https://yourdomain.com
API_URL=https://api.yourdomain.com
BASE_URL=https://yourdomain.com

# GitHub OAuth App for Production
# Create separate app at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_production_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_production_github_oauth_client_secret

# ----------------------------------------------------------------------------
# 💳 STRIPE (USE LIVE MODE KEYS)
# ----------------------------------------------------------------------------
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_YOUR_STRIPE_LIVE_SECRET_KEY
STRIPE_WEBHOOK_SECRET=whsec_YOUR_STRIPE_LIVE_WEBHOOK_SECRET
STRIPE_PRICE_PRO=price_YOUR_STRIPE_LIVE_PRO_PRICE_ID
STRIPE_PRICE_ENTERPRISE=price_YOUR_STRIPE_LIVE_ENTERPRISE_PRICE_ID

# ----------------------------------------------------------------------------
# 🤖 AI FEATURES
# ----------------------------------------------------------------------------
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-YOUR_OPENAI_API_KEY
AI_MODEL=gpt-4o-mini
AI_ANALYSIS_INTERVAL_MS=300000
AI_LOOKBACK_MINUTES=5
AI_MAX_TOKENS=500
AI_TEMPERATURE=0.7

# ----------------------------------------------------------------------------
# 🛠️  ENVIRONMENT
# ----------------------------------------------------------------------------
NODE_ENV=production
