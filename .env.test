# Test Database Configuration
# This file is used automatically by the test suite (referenced in package.json test scripts)
# Copy this file to .env.test and update with your local PostgreSQL credentials

# PostgreSQL connection string for running contract tests
# Format: postgresql://[user[:password]@][host][:port][/dbname][?param1=value1&...]
DATABASE_URL=postgresql://cronicorn_user:asdfasdfasdf@localhost:5432/cronicorn_prod

# Example configurations:
# Local PostgreSQL: postgresql://postgres:password@localhost:5432/cronicorn_test
# Docker: postgresql://postgres:postgres@localhost:5432/cronicorn_test
# Supabase: postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres
