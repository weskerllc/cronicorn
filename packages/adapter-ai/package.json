{
  "name": "@cronicorn/adapter-ai",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./types": "./dist/types/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b -w",
    "test": "vitest run",
    "lint": "eslint .",
    "lint:fix": "eslint . --max-warnings 0 --fix",
    "clean": "rm -rf node_modules .turbo dist"
  },
  "dependencies": {
    "ai": "^5.0.0",
    "zod": "^4.1.12",
    "zod-to-json-schema": "^3.22.0",
    "msw": "^2.11.3"
  },
  "peerDependencies": {
    "@cronicorn/scheduler": "workspace:*"
  }
}
