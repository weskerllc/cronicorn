# All variables are optional - the app has working defaults
# Copy this file to .env and uncomment what you want to customize

# Docker Compose
DB_PORT=6666
DB_RESTART=unless-stopped

# Database Container (used by postgres container)
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=db

# Database Connection (for local dev - points to Docker DB on localhost:6666)
DATABASE_URL=postgresql://user:password@localhost:6666/db

# Better Auth (required for API)
# Generate secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=dev-secret-key-must-be-at-least-32-characters-long
BETTER_AUTH_URL=http://localhost:3333
WEB_URL=http://localhost:5173
API_URL=http://localhost:3333

# GitHub OAuth (optional - only required if admin user is not configured)
# Create at: https://github.com/settings/developers
# Leave blank to use admin user authentication instead
GITHUB_CLIENT_ID=your_github_oauth_app_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_app_client_secret

# Admin User (optional - for CI/testing environments without OAuth)
# When configured, allows instant login without GitHub OAuth
# Both ADMIN_USER_EMAIL and ADMIN_USER_PASSWORD must be set together
# Either GitHub OAuth OR Admin User must be configured
# ADMIN_USER_EMAIL=admin@example.com
# ADMIN_USER_PASSWORD=your-secure-password-min-8-chars
# ADMIN_USER_NAME=Admin User

# Server
PORT=3333
NODE_ENV=development

# AI Scheduler (optional - leave blank to disable AI features)
# Get your key at: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your_openai_api_key_here
# AI_MODEL=gpt-4o-mini

# Stripe Payments (required for subscriptions)
# Setup guide: /scripts/README.md

# 1. Get test API key from: https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# 2. Run setup script: pnpm tsx scripts/setup-stripe-products.ts
STRIPE_PRICE_PRO=price_your_pro_monthly_price_id
STRIPE_PRICE_ENTERPRISE=price_your_enterprise_monthly_price_id

# 3. Get webhook secret from: stripe listen --forward-to localhost:3333/api/webhooks/stripe
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_signing_secret_here

# 4. For production, use your production domain
BASE_URL=http://localhost:5173

# Web UI Configuration
# Website URL for UI (used by Vite in web app)
VITE_SITE_URL=http://localhost:5173
